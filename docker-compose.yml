version: '3'

services:
  postgres:
    image: postgres
    restart: always
    volumes:
      - data:/data/db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: cms
    ports:
      - "5432:5432"

volumes:
  data:
